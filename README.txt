TAK Duty Control V4

اصلاحات:
- تاریخ ورودی کاملاً شمسی است؛ دیگر فیلد date میلادی ندارد.
- وظایف دکمه ادیت دارند؛ فقط مدیرعامل یا ایجادکننده وظیفه می‌تواند ادیت کند.
- جدول موبایل یک‌خطی و بدون اسکرول افقی است.
- روی موبایل فقط ستون‌های وضعیت، وظیفه، مسئول، تاریخ و عمل دیده می‌شود.
- عملیات روی موبایل داخل سه‌نقطه است.
- روی دسکتاپ جدول کامل دیده می‌شود.

نکته مهم:
چون کلید ذخیره‌سازی عوض شده، بعد از آپلود نسخه جدید دیتای تست جدید ساخته می‌شود.
